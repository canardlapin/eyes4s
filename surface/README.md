# eyes4s-surface

Density estimation: smoothers, bandwidth selection, pyramids, entropy.

The Gaussian smoother is a separable convolution, so this module needs no
linear-algebra dependency.

No code yet. See `../PRD.md` for this module's requirements and `../.mote/` for its work items.
